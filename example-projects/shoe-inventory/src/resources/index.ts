// Resource definitions for the SoleTrack inventory system
// The factory reads these to generate API routes, database tables, and types

export { brandResource } from './brand.resource'
export { productResource } from './product.resource'
